# Copy to .env for local dev. These are the defaults the app falls back to
# anyway (see src/lib/api.ts and src/hooks/useStockSocket.ts).
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
VITE_APP_NAME=Stocksense
